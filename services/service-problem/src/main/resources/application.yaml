spring:
    application:
        name: service-problem
    cloud:
        nacos:
            server-addr: 127.0.0.1:8848
            config:
                import-check:
                    enabled: false
    datasource:
        url: jdbc:mysql://localhost:3306/problem?characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
        username: root
        password: 123456
        driver-class-name: com.mysql.cj.jdbc.Driver
    data:
        redis:
            host: localhost
            port: 6379
            database: 0
server:
    port: 8000
