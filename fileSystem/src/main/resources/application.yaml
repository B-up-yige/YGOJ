spring:
    application:
        name: fileSystem
    cloud:
        nacos:
            server-addr: 192.168.61.135:8848
            config:
                import-check:
                    enabled: false
    data:
        redis:
            host: 192.168.61.135
            port: 6379
            database: 0
    servlet:
        multipart:
            max-file-size: 100MB
            max-request-size: 200MB

# 文件存储根目录
filesystem:
    root:
        path: file

server:
    port: 9201
